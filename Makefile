deploy:
	ssh infomaniaknode 'export NVM_DIR="$$HOME/.nvm" && [ -s "$$NVM_DIR/nvm.sh" ] && . "$$NVM_DIR/nvm.sh" && nvm install 24 && nvm use 24 && node -v && npm -v && cd ~/sites/pokedex.blizzardaudioclub.ch && git fetch origin master && git checkout -f master && git reset --hard origin/master && git clean -fd && npm i && npm run build'